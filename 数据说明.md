# PoE2 做装模拟器数据包

来源：GitHub 开源项目，数据对应游戏 0.5 版本（2026-07 从 poe2db.tw 提取）。

## 文件清单

主要数据来自 [Dboire9/POE2_HTC](https://github.com/Dboire9/POE2_HTC)（AGPL-3.0，Java 引擎 + React 前端的做装模拟器）：

| 文件 | 内容 | 规模 |
|---|---|---|
| `currencies.json` | 核心做装通货定义（蜕变/增幅/富豪/崇高/剥离石、精华、亵渎通货），含对应引擎类名、词缀来源池、可搭配预兆 | 7 种通货 × 4 强度档 |
| `currency_tiers.json` | 普通/高级/完美分级规则（从引擎代码提取，含档位下限和分级价格） | 3 档 |
| `omens.json` | 预兆（Omen）目录：作用、限制前后缀、对应概率函数、验证状态 | 13 种 |
| `essences.json` | 精华目录：每种精华各等级（Lesser/Normal/Greater/Perfect）映射到部位×词缀 | 21 种 |
| `prices.json` | 通货参考价格（崇高石 = 1），含高级/完美版本和预兆附加成本 | 手工估值 |
| `mods.json` | 全部词缀：词缀池、前后缀、等级(ilvl)要求、权重、数值范围、词缀族 | 1351 条 |
| `base_items.json` | 装备基底分类及各自可出的前缀/后缀池 | 41 类 |
| `weights_overrides.json` | 权重覆盖表（对特定词缀的手工修正） | 少量 |
| `bases.json` | 具体武器基底名录（9 类 / 271 个，中英文名 + 基础属性，poe2db 0.5 手工提取，2026-08 补充） | 271 |

参考对照数据来自 [quicktime/poe-2-toolkit](https://github.com/quicktime/poe-2-toolkit)（GPL-3.0）：

| 文件 | 内容 | 备注 |
|---|---|---|
| `poe-2-toolkit_currency.json` | 通货 + 碎片 + 精华列表（含掉落等级、稀有度分级） | 精华部分是未清洗的原始文本，质量一般，仅作对照 |

## 关键机制速记

- **高级/完美通货**：机制是抬高词缀档位下限——高级只能 roll 词缀档 ilvl ≥ 35 的档位，完美 ≥ 50，详见 `currency_tiers.json`。
- **混沌石**：引擎建模为"随机删一条词缀 + 当一次崇高石加一条"，分级参数透传生效，但无单独定价。
- **剥离石**：随机删一条词缀（1/当前词缀数），可搭配预兆限定删前缀/后缀。
- **精华**：按 Lesser/Normal/Greater/Perfect 分层，词缀档位即精华等级，与球类通货的普通/高级/完美是两套体系。

## 数据时效

对应 0.5 版本。上游更新后可用 POE2_HTC 仓库 `tools/refresh/` 脚本从 poe2db.tw 重新提取。


## 派生产物

- `app/data.js`：由 `tools/build_data.py` 把上述 JSON 合并打包的前端数据包（运行 `python tools/build_data.py` 重新生成）。
